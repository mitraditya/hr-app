---
name: Deep audit — 2026-06-08
description: Comprehensive project audit findings, problems found, and actions taken
type: project
originSessionId: f9fb8799-c860-4f92-88f7-d834cf191da5
---
Audit performed 2026-06-08 on the `feat/admin-verify-employee` branch.

## State at time of audit

- **Backend**: Supabase (fully migrated from PocketBase in May 2026)
- **Frontend**: React 19 + TypeScript + Vite, PWA-enabled
- **Branch**: feat/admin-verify-employee
- **Last deploy**: 2026-06-08 (cron job fix)

## Problems found and fixed during this audit

### Remaining PocketBase API calls (all migrated in this session)
1. **AdBanner.tsx** — was calling `apiClient.pb?.send(/api/openhr/ad-config/{slot})`. Migrated to direct Supabase settings table query.
2. **PublicAdBanner.tsx** — was calling `apiClient.pb?.send(/api/openhr/public-ad-config/{slot})`. Migrated to new Supabase Edge Function `public-ad-config`.
3. **middleware.ts** — was fetching blog/tutorial meta from `https://pocketbase.mimnets.com`. Migrated to Supabase REST API.
4. **scripts/generate-sitemap.mjs** — was fetching from PB. Migrated to Supabase REST API.
5. **scripts/generate-feed.mjs** — was fetching from PB. Migrated to Supabase REST API.

### PocketBase dependency removed
- Removed `pocketbase` npm package (0.25.0) from package.json
- Removed PB import map from index.html
- Removed `prebuild` (validate-pb-hooks) and `validate:hooks` npm scripts
- Converted `pocketbase.ts` to a stub (backward compatibility for imports)
- Updated `api.client.ts` to remove PB references
- Updated `database.ts` to reflect Supabase-only backend

### Files cleaned up (deleted)
- 145MB PB database backup: `Others/@auto_pb_backup_open_hr_app_20260504000000/`
- Duplicate hook copies: `pb_hooks/Fix-2/`, `pb_hooks/Leave WorkFlow Fix/`
- Backup files: `main.pb.js.claude.js`, `bk_mail.pb.js`, `bk.cron.pb.js`, `03.02.26-bk.main.pb.js`
- Root junk: `test.md`, `FIXES_SUMMARY.md`, `AUTO_CLOSE_ANALYSIS.md`
- Old artifacts: `Claude_Prompt/`, `PSD/`, `guides-preview.html`, `build-guides-preview.cjs`
- `pb_hooks_registration_example.pb.js`
- All `.DS_Store` files

### New Supabase Edge Function created
- `supabase/functions/public-ad-config/index.ts` — serves ad config to unauthenticated clients (landing page, blog, tutorials). **Needs deployment.**

### Known remaining issues (not addressed in this audit)
- **AuthContext.tsx** still uses `isPocketBaseConfigured()` (now stub, always returns true) — cosmetic, harmless
- **Login.tsx** still shows "Node Connected" indicator using PB config (always shows connected) — cosmetic
- **Setup.tsx** still references PB config (page is never shown since isConfigured is always true)
- **SearchDialog.tsx** has PB-related comments in catch blocks — cosmetic
- **Organization.service.ts** has `testPocketBaseConnection` alias (delegates to supabase) — cosmetic
- **validate-pb-hooks.cjs** file still exists but is no longer referenced by any npm script
- **scripts/migrate-from-pb/** still exists with 5 migration scripts (historical reference)
- **pb_hooks/** directory still contains 5 active PB hook files (cron.pb.js, main.pb.js, attendance_notifications.pb.js, review_notifications.pb.js, leave_notifications.pb.js, setup_collections.pb.js) — kept as reference since PB server may still be running

### Notes on middleware and build scripts
- The Supabase anon key is hardcoded in middleware.ts and the build scripts (generate-sitemap.mjs, generate-feed.mjs). This is intentional — they are public keys and the scripts need them at build/runtime.
- If the anon key is rotated, these files must be updated.
